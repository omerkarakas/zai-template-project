---
phase: 02-page-structure-content
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/services/ServicePageLayout.tsx
  - src/components/services/ServiceBenefits.tsx
  - src/components/services/ServiceProcess.tsx
autonomous: true

must_haves:
  truths:
    - "ServicePageLayout component renders consistent structure for all service pages"
    - "ServiceBenefits component displays benefits in grid/list layout"
    - "ServiceProcess component displays process steps visually"
    - "All components accept props for dynamic content"
  artifacts:
    - path: "src/components/services/ServicePageLayout.tsx"
      provides: "Reusable layout wrapper for service pages"
      min_lines: 50
      exports: ["ServicePageLayout"]
    - path: "src/components/services/ServiceBenefits.tsx"
      provides: "Benefits display component with icon and text"
      min_lines: 40
      exports: ["ServiceBenefits"]
    - path: "src/components/services/ServiceProcess.tsx"
      provides: "Process steps visualization component"
      min_lines: 50
      exports: ["ServiceProcess"]
  key_links:
    - from: "ServicePageLayout.tsx"
      to: "ServiceBenefits.tsx, ServiceProcess.tsx"
      via: "Component composition in children prop"
      pattern: "import.*ServiceBenefits"
---

<objective>
Create reusable service page components that provide consistent structure for all 6 service pages.

Purpose: Avoid duplication across 6 service pages by creating shared components for layout, benefits display, and process visualization. This ensures consistency and reduces maintenance burden.

Output: Three service page components (ServicePageLayout, ServiceBenefits, ServiceProcess) ready to be used in all service pages.
</objective>

<execution_context>
@C:\Users\Omer\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Omer\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-page-structure-content/02-CONTEXT.md
@.planning/phases/01-project-setup/01-01-SUMMARY.md
@src/lib/utils.ts
@tailwind.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ServicePageLayout component</name>
  <files>src/components/services/ServicePageLayout.tsx</files>
  <action>
Create a reusable layout component that provides consistent structure for all service pages.

**Component Props (TypeScript interface):**
```typescript
interface ServicePageLayoutProps {
  title: string;           // Service page title
  description: string;     // Brief description/subtitle
  icon?: React.ReactNode;  // Optional icon component
  children: React.ReactNode; // Main content area
}
```

**Component Structure:**
1. Hero section at top:
   - Icon (if provided) with gradient background
   - Title (h1, large, bold)
   - Description (paragraph, muted text)
2. Main content area (children) with:
   - Max-width container for readability
   - Proper padding and spacing
3. CTA section at bottom (optional, can be in children):
   - Contact button/CTA

**Styling:**
- Use Tailwind classes with CSS variables
- Gradient accent for icon background
- Proper spacing between sections
- Center-align hero text for impact
- Use container with max-width for main content

**File location:** src/components/services/ServicePageLayout.tsx

This component will be imported and used in all 6 service pages to ensure consistent layout.
</action>
  <verify>
1. src/components/services/ServicePageLayout.tsx exists
2. Component exports ServicePageLayout with TypeScript interface
3. Props include title, description, optional icon, and children
4. Component renders hero section and content area
5. Uses Tailwind classes and CSS variables
  </verify>
  <done>
ServicePageLayout component created with hero section, content area, and proper TypeScript types. Ready for use in all service pages.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create ServiceBenefits component</name>
  <files>src/components/services/ServiceBenefits.tsx</files>
  <action>
Create a component to display service benefits in a visually appealing grid layout.

**Component Props:**
```typescript
interface Benefit {
  title: string;
  description: string;
  icon?: React.ReactNode;
}

interface ServiceBenefitsProps {
  benefits: Benefit[];
  title?: string;  // Optional section title, defaults to "Faydalar"
}
```

**Component Structure:**
1. Section title (h2) if provided or default "Faydalar"
2. Grid layout for benefits:
   - Responsive: 1 column mobile, 2 columns tablet, 3 columns desktop
   - Each benefit card contains:
     - Icon (if provided) or default checkmark icon
     - Benefit title (h3)
     - Benefit description (paragraph)
3. Card styling:
   - Subtle border
   - Light background (card color)
   - Rounded corners
   - Hover effect (subtle lift or border color change)
   - Proper padding

**Styling:**
- Use Tailwind grid: grid-cols-1 md:grid-cols-2 lg:grid-cols-3
- Use lucide-react icons (CheckCircle or similar) for default icons
- Use CSS variables for colors
- Add gap between cards

**File location:** src/components/services/ServiceBenefits.tsx
</action>
  <verify>
1. src/components/services/ServiceBenefits.tsx exists
2. Component exports ServiceBenefits with TypeScript interfaces
3. Props include benefits array and optional title
4. Component renders benefits in responsive grid layout
5. Each benefit card has icon, title, and description
  </verify>
  <done>
ServiceBenefits component created with responsive grid layout, card styling, and TypeScript types. Ready to display benefits for any service.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create ServiceProcess component</name>
  <files>src/components/services/ServiceProcess.tsx</files>
  <action>
Create a component to visualize the service delivery process with numbered steps.

**Component Props:**
```typescript
interface ProcessStep {
  step: number;        // Step number (1, 2, 3, etc.)
  title: string;       // Step title
  description: string; // Step description
}

interface ServiceProcessProps {
  steps: ProcessStep[];
  title?: string;  // Optional section title, defaults to "Süreç"
}
```

**Component Structure:**
1. Section title (h2) if provided or default "Süreç"
2. Steps display with two layout options (choose one):
   - **Option A (Horizontal):** Steps in horizontal row with arrows between
   - **Option B (Vertical):** Steps in vertical timeline with connecting line
3. Each step contains:
   - Step number in circle/badge with gradient background
   - Step title (h3)
   - Step description (paragraph)
4. Visual indicators:
   - Arrow or line connecting steps
   - Numbered badges (1, 2, 3...)
   - Active/hover states for interactivity

**Styling:**
- Use Tailwind flex or grid for layout
- Gradient background for step numbers
- Use CSS variables for colors
- Add gap between steps
- Responsive: horizontal on desktop, vertical on mobile

**File location:** src/components/services/ServiceProcess.tsx

Recommendation: Use Option B (vertical timeline) for better mobile responsiveness and clearer visual flow.
</action>
  <verify>
1. src/components/services/ServiceProcess.tsx exists
2. Component exports ServiceProcess with TypeScript interfaces
3. Props include steps array and optional title
4. Component renders steps with numbered badges
5. Steps are connected visually (arrows or timeline)
  </verify>
  <done>
ServiceProcess component created with step visualization, numbered badges, and connecting indicators. Ready to display process flow.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. All three components exist in src/components/services/
2. Each component has proper TypeScript interfaces
3. Components are composable and reusable
4. Components use Tailwind classes and CSS variables
5. Components accept props for dynamic content
</verification>

<success_criteria>
1. ServicePageLayout provides consistent wrapper for service pages
2. ServiceBenefits displays benefits in responsive grid
3. ServiceProcess visualizes process steps with numbered badges
4. All components have TypeScript types
5. Components are ready to be used in all 6 service pages
</success_criteria>

<output>
After completion, create `.planning/phases/02-page-structure-content/02-02-SUMMARY.md`
</output>
