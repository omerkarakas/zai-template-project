---
phase: 02-page-structure-content
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/components/ui/button.tsx
  - src/components/layout/Header.tsx
  - src/components/layout/Footer.tsx
autonomous: false

must_haves:
  truths:
    - "Brand colors (gradient + accent) are applied via CSS variables in globals.css"
    - "Button component renders with variants (default, outline, ghost) and sizes"
    - "Header component displays Moka Dijital logo and navigation menu"
    - "Footer component displays contact information and copyright"
    - "Navigation menu links to all service pages"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Brand color CSS variables with gradient and accent colors"
      min_lines: 80
      contains: "--primary", "--accent", "gradient"
    - path: "src/components/ui/button.tsx"
      provides: "Reusable Button component with shadcn/ui variants"
      min_lines: 40
      exports: ["Button"]
    - path: "src/components/layout/Header.tsx"
      provides: "Site header with logo and navigation"
      min_lines: 50
      exports: ["Header"]
    - path: "src/components/layout/Footer.tsx"
      provides: "Site footer with contact info"
      min_lines: 40
      exports: ["Footer"]
  key_links:
    - from: "src/components/layout/Header.tsx"
      to: "/hizmetler/*"
      via: "Link components in navigation menu"
      pattern: 'href="/hizmetler/'
---

<objective>
Establish design system foundation with Moka Dijital brand colors and create reusable navigation components (Header, Footer) and Button component.

Purpose: Create consistent visual language across all pages with gradient + accent color scheme per Phase 2 context, and build navigation components that will be used throughout the site.

Output: Updated globals.css with brand colors, shadcn/ui Button component, Header and Footer components ready for use in all pages.
</objective>

<execution_context>
@C:\Users\Omer\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Omer\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-page-structure-content/02-CONTEXT.md
@.planning/phases/01-project-setup/01-01-SUMMARY.md
@src/app/globals.css
@tailwind.config.ts
@components.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update globals.css with Moka Dijital brand colors</name>
  <files>src/app/globals.css</files>
  <action>
Update the CSS variables in :root to implement Moka Dijital's brand colors following the design language from CONTEXT.md:

**Color Strategy:**

- Primary: Deep blue/indigo base (technology trust)
- Accent: Vibrant gradient (purple to blue, or blue to cyan)
- Keep slate/neutral for text and backgrounds
- Add gradient utility classes

**Specific changes:**

1. Modify --primary HSL values to use blue/indigo range (around 220-240 hue)
2. Modify --accent HSL values to use vibrant purple/cyan range (around 260-290 hue)
3. Add gradient CSS variables:
   - --gradient-from: HSL for gradient start
   - --gradient-to: HSL for gradient end
   - --gradient-text: HSL for gradient text effect
4. Add utility class .gradient-text in @layer utilities for text with gradient
5. Add utility class .gradient-bg in @layer utilities for background gradient

Do NOT change the existing CSS variable structure (HSL format with light/dark mode). Only update the HSL values and add new gradient variables.

The design should be modern and clean with:

- White/light backgrounds as base
- Gradient accents for CTAs and highlights
- Subtle shadows and rounded corners
  </action>
  <verify>

1. File contains new --gradient-from, --gradient-to, --gradient-text variables
2. .gradient-text and .gradient-bg utility classes exist in @layer utilities
3. Primary color is blue/indigo range (hue 220-240)
4. Accent color is vibrant (hue 260-290 or similar)
5. Both light and dark modes have updated values
   </verify>
   <done>
   CSS variables updated with Moka Dijital brand colors including gradient support. Theme is modern with blue/indigo primary and vibrant accent colors.
   </done>
   </task>

<task type="auto">
  <name>Task 2: Create shadcn/ui Button component</name>
  <files>src/components/ui/button.tsx</files>
  <action>
Create the Button component using shadcn/ui pattern:

1. Use the command: `npx shadcn@latest add button`
2. If command prompts for confirmation, accept defaults
3. The component will be created at src/components/ui/button.tsx

If the shadcn command fails, manually create the button component following shadcn/ui patterns:

- Use class-variance-authority for variants
- Use cn() utility from @/lib/utils
- Include variants: default, outline, ghost, link
- Include sizes: default, sm, lg, icon
- Use Tailwind classes with CSS variables (primary, secondary, accent, etc.)

After creation, verify the component exports Button and has proper TypeScript types.
</action>
<verify>

1. src/components/ui/button.tsx exists
2. File contains Button component with variants and sizes
3. Component uses class-variance-authority and cn() utility
4. TypeScript exports are correct (export const Button)
   </verify>
   <done>
   Button component installed/created with all variants (default, outline, ghost, link) and sizes (default, sm, lg, icon) ready for use.
   </done>
   </task>

<task type="auto">
  <name>Task 3: Create Header and Footer layout components</name>
  <files>src/components/layout/Header.tsx src/components/layout/Footer.tsx</files>
  <action>
Create two layout components for consistent site navigation.

**Header.tsx:**

- Logo/brand name: "Moka Dijital" (Link to home="/")
- Navigation menu with links to all 6 service pages:
  - Google İşletme Optimizasyonu (/hizmetler/google-isletme)
  - Yerel SEO (/hizmetler/seo)
  - Potansiyel Müşteri Kazanımı (/hizmetler/pmk)
  - Web Geliştirme (/hizmetler/web-gelistirme)
  - Dijital Reklam Yönetimi (/hizmetler/reklam)
  - Otomasyon (/hizmetler/otomasyon)
- Use Next.js Link component for navigation
- Responsive design: desktop horizontal menu, mobile hamburger menu (use shadcn/ui Sheet or Dialog for mobile menu if needed, or simple CSS-based toggle)
- Sticky header option (add sticky top-0 if desired)
- Use z-index for proper layering

**Footer.tsx:**

- Contact information section:
  - Email: iletisim@mokadijital.com (or actual email if known)
  - Phone: Display phone number
  - Location: Turkey city/region
- Copyright notice with current year
- Optional: Quick links to service pages
- Simple, clean design matching header style

**Styling:**

- Use Tailwind classes
- Use CSS variables for colors (bg-background, text-foreground, border-border)
- Add subtle shadow to header for depth
- Use appropriate spacing (padding, margins)

Create both components in src/components/layout/ directory.
</action>
<verify>

1. src/components/layout/Header.tsx exists and exports Header
2. src/components/layout/Footer.tsx exists and exports Footer
3. Header contains logo and 6 navigation links
4. Footer contains contact information and copyright
5. Both components use Next.js Link for navigation
6. Mobile menu functionality is present (hamburger toggle)
   </verify>
   <done>
   Header component with logo, 6 service page navigation links, and mobile menu. Footer component with contact info and copyright. Both ready for use in layouts.
   </done>
   </task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Task 1-3: Complete design system foundation with:
- Updated globals.css with Moka Dijital brand colors (gradient + accent)
- shadcn/ui Button component with all variants
- Header component with logo and navigation menu
- Footer component with contact information
  </what-built>
  <how-to-verify>
1. Start dev server: `npm run dev`
2. Visit http://localhost:3000
3. Check that the page loads without errors
4. Inspect browser DevTools -> Elements -> Computed styles to verify new CSS variables are applied
5. Check that Header displays "Moka Dijital" and navigation links
6. Check mobile responsiveness (resize browser to mobile width) - hamburger menu should work
7. Check Footer displays contact information
8. Verify colors match the modern, clean design with gradient accents
  </how-to-verify>
  <resume-signal>Type "approved" if design system looks good, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
After all tasks complete:
1. npm run dev starts without errors
2. CSS variables for brand colors are defined and applied
3. Button component is available and renders
4. Header displays logo and 6 navigation links
5. Footer displays contact info and copyright
6. Mobile menu toggles correctly
7. Colors are modern with gradient accents as specified
</verification>

<success_criteria>

1. Moka Dijital brand colors implemented with gradient + accent approach
2. Button component available with all variants and sizes
3. Header and Footer components created and render correctly
4. Navigation menu links to all 6 service pages
5. Mobile-responsive navigation working
6. Design system ready for use in homepage and service pages
   </success_criteria>

<output>
After completion, create `.planning/phases/02-page-structure-content/02-01-SUMMARY.md`
</output>
