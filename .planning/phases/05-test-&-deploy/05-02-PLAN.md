---
phase: 05-test-&-deploy
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - TESTING.md
  - src/app/layout.tsx
autonomous: false

must_haves:
  truths:
    - "All pages responsive on mobile (320px - 768px), tablet (768px - 1024px), and desktop (1024px+)"
    - "Navigation menu accessible on all screen sizes"
    - "Contact modal displays correctly on mobile devices"
    - "Forms are fillable and submit on mobile viewports"
    - "No horizontal scrolling on any page"
    - "Cross-browser compatibility verified for Chrome, Firefox, Safari"
  artifacts:
    - path: "TESTING.md"
      provides: "Testing documentation and checklist"
      min_lines: 80
    - path: "src/app/layout.tsx"
      provides: "Root layout with viewport meta tag"
      contains: "viewport"
  key_links:
    - from: "TESTING.md"
      to: "all pages"
      via: "Manual testing checklist"
      pattern: "responsive|mobile|tablet|desktop"
---

<objective>
Perform comprehensive responsive and cross-browser testing to verify site works correctly on all devices and browsers before production deployment.

Purpose: Ensure user experience is consistent across mobile, tablet, desktop devices and major browsers. Output includes testing documentation, viewport meta tag verification, and responsive design checklist.
</objective>

<execution_context>
@C:\Users\Omer\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Omer\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/layout.tsx
@tailwind.config.ts

# Phase 2 Summaries (responsive design patterns)
@.planning/phases/02-page-structure-content/02-01-SUMMARY.md
@.planning/phases/02-page-structure-content/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify viewport meta tag in root layout</name>
  <files>src/app/layout.tsx</files>
  <action>
    Verify viewport meta tag is properly configured in root layout:

    1. Read src/app/layout.tsx
    2. Check for viewport metadata in the Metadata export
    3. Ensure viewport configuration includes:
       - width=device-width
       - initial-scale=1
       - (optional) maximum-scale=5 for iOS zoom

    Expected format in metadata:
    ```typescript
    viewport: {
      width: 'device-width',
      initialScale: 1,
    }
    ```

    If viewport is missing or incomplete, add proper viewport configuration to ensure mobile browsers render correctly.

    Do NOT modify other metadata or layout structure. Only verify/add viewport.
  </action>
  <verify>src/app/layout.tsx contains viewport configuration with width=device-width and initialScale=1</verify>
  <done>Viewport meta tag ensures proper mobile rendering across all devices</done>
</task>

<task type="auto">
  <name>Task 2: Create TESTING.md documentation</name>
  <files>TESTING.md</files>
  <action>
    Create TESTING.md file with comprehensive testing checklist:

    1. **Responsive Testing** (device breakpoints):
       - Mobile: 320px, 375px, 414px (iPhone SE, iPhone 12/13/14 Pro)
       - Tablet: 768px, 834px (iPad Mini, iPad Pro)
       - Desktop: 1024px, 1280px, 1440px, 1920px

    2. **Test on Each Page** (7 pages):
       - Homepage (/)
       - Google İşletme (/hizmetler/google-isletme-optimizasyonu)
       - SEO (/hizmetler/seo)
       - PMK (/hizmetler/pmk)
       - Web Geliştirme (/hizmetler/web-gelistirme)
       - Reklam (/hizmetler/reklam)
       - Otomasyon (/hizmetler/otomasyon)

    3. **Checklist Items per Page**:
       - Header logo visible and properly sized
       - Navigation menu accessible (hamburger menu on mobile if applicable)
       - Hero section text readable and properly aligned
       - Service cards/content sections stack vertically on mobile
       - Contact form button visible and tappable
       - Footer information readable
       - No horizontal scrolling (check body width)
       - Images scale correctly (use Next.js Image component with sizes prop)
       - Modal opens and closes properly on touch devices

    4. **Browser Testing**:
       - Chrome (latest)
       - Firefox (latest)
       - Safari (iOS and macOS if available)
       - Edge (if available)

    5. **Form Testing on Mobile**:
       - Form fields are tappable (min 44x44px touch targets)
       - Keyboard doesn't hide form inputs
       - Validation messages visible
       - Submit button accessible

    6. **DevTools Instructions**:
       - Chrome DevTools: Toggle device toolbar (Ctrl+Shift+M)
       - Test in landscape and portrait orientations
       - Check Network tab for 404s or slow resources
       - Lighthouse audit for performance score

    Use checkboxes for manual testing items.
  </action>
  <verify>TESTING.md exists with responsive testing checklist for 7 pages, 4+ breakpoints, browser testing section, and mobile form testing</verify>
  <done>Complete testing documentation with responsive design checklist for manual verification</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Viewport meta tag verified and TESTING.md documentation created with comprehensive responsive design checklist</what-built>
  <how-to-verify>
    1. Run development server: npm run dev
    2. Open Chrome DevTools (F12) and toggle device toolbar (Ctrl+Shift+M)
    3. Test homepage at breakpoints:
       - Mobile: 375px (iPhone 12/13)
       - Tablet: 768px (iPad)
       - Desktop: 1440px (standard desktop)
    4. Verify for each breakpoint:
       - Header navigation accessible
       - Hero text readable and properly aligned
       - Service cards/content stack vertically on mobile
       - No horizontal scrolling
       - Contact button tappable
    5. Test 2-3 service pages (e.g., /hizmetler/google-isletme-optimizasyonu, /hizmetler/pmk)
    6. Test contact modal on mobile view (click button, verify modal fits screen)
    7. Check landscape orientation on mobile (rotate device in DevTools)

    Expected: All pages display correctly at all breakpoints with no horizontal scrolling, readable text, and accessible navigation.
  </how-to-verify>
  <resume-signal>Type "approved" if responsive testing passes, or describe issues (e.g., "Navigation menu broken on mobile", "Modal too large on 375px")</resume-signal>
</task>

</tasks>

<verification>
Overall phase checks:
1. Viewport meta tag configured for proper mobile rendering
2. TESTING.md covers all 7 pages, 4+ breakpoints, browser testing
3. Responsive design verified on mobile, tablet, desktop viewports
4. No horizontal scrolling on any page
5. Navigation and forms accessible on mobile devices
6. Contact modal displays correctly on small screens
</verification>

<success_criteria>
- Viewport meta tag ensures mobile browsers render at correct scale
- Testing documentation provides clear checklist for responsive design verification
- Site is responsive on mobile (320px-768px), tablet (768px-1024px), and desktop (1024px+)
- Navigation, forms, and modals work correctly on touch devices
- No horizontal scrolling or layout breaks at any breakpoint
</success_criteria>

<output>
After completion, create `.planning/phases/05-test-&-deploy/05-02-SUMMARY.md`
</output>
