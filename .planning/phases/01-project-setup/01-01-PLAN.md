---
phase: 01-project-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - next.config.mjs
  - tsconfig.json
  - tailwind.config.ts
  - postcss.config.mjs
  - .eslintrc.json
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/globals.css
autonomous: true

must_haves:
  truths:
    - "Next.js 15 project builds successfully with `output: 'export'` configuration"
    - "Development server starts without errors using `npm run dev`"
    - "Static export build completes successfully with `npm run build`"
    - "TypeScript compilation passes without type errors"
    - "Tailwind CSS classes are applied and working"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies and scripts"
      contains: ["next@15", "react@19", "typescript", "tailwindcss", "@radix-ui", "react-hook-form", "zod", "next-export-optimize-images"]
      min_lines: 30
    - path: "next.config.mjs"
      provides: "Next.js configuration with static export"
      contains: "output: 'export'"
      exports: []
    - path: "src/app/layout.tsx"
      provides: "Root layout with HTML structure"
      min_lines: 20
    - path: "src/app/page.tsx"
      provides: "Homepage component"
      min_lines: 10
    - path: "tailwind.config.ts"
      provides: "Tailwind CSS configuration"
      contains: "content"
      min_lines: 15
  key_links:
    - from: "package.json"
      to: "next.config.mjs"
      via: "next-export-optimize-images plugin"
      pattern: "next-export-optimize-images"
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "import statement"
      pattern: "import.*globals.css"
---

<objective>
Initialize Next.js 15 project with TypeScript, Tailwind CSS, and static export configuration for PHP hosting deployment.

Purpose: Establish the foundational project structure that supports static HTML/CSS/JS export, enabling deployment to PHP hosting without Node.js server requirements.

Output: Working Next.js project with all core dependencies installed, configured for static export, with basic page structure and styling system.
</objective>

<execution_context>
@C:\Users\Omer\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Omer\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-project-setup/01-CONTEXT.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md

# Decisions from CONTEXT.md (LOCKED - honor exactly):
- UI Component Library: Shadcn/ui (copy-paste components with Radix UI + Tailwind)
- Form Handling: React Hook Form + Zod for validation
- Theming: Custom theme colors (extending Tailwind config)
</context>

<tasks>

<task type="auto">
  <name>Initialize Next.js project with core dependencies</name>
  <files>
    package.json
    next.config.mjs
    tsconfig.json
  </files>
  <action>
    1. Initialize Next.js 15 project with TypeScript:
       - Create package.json with Next.js 15.1.6, React 19, TypeScript 5.7, Tailwind CSS 3.4
       - Include next-export-optimize-images@3.4.0 for build-time image optimization
       - Add React Hook Form 7.54.2, Zod 3.24.1, @hookform/resolvers for forms
       - Add @radix-ui/* packages (dialog, dropdown-menu, tooltip) for accessible UI primitives
       - Add lucide-react for icons, clsx + tailwind-merge for class merging
       - Add framer-motion 11.12.0 for animations, next-themes 0.4.4 for theming
       - Include dev dependencies: typescript, @types/*, eslint, prettier, tailwindcss, postcss, autoprefixer

    2. Create next.config.mjs with:
       - output: 'export' for static export (CRITICAL for PHP hosting)
       - Configure next-export-optimize-images plugin wrapper
       - Set images.unoptimized: false (plugin handles optimization)
       - Configure deviceSizes and imageSizes for responsive images
       - Set trailingSlash: false for clean URLs

    3. Create tsconfig.json with:
       - target: ES2022, lib: dom/dom.iterable/esnext
       - strict mode enabled
       - path alias: "@/*" -> "./src/*"
       - Strong typing options: noUnusedLocals, noUnusedParameters, noFallthroughCasesInSwitch

    DO NOT use: API routes, Server Actions, getServerSideProps, ISR, Middleware (incompatible with static export)
  </action>
  <verify>
    npm install completes without errors
    package.json contains next@15.1.6, react@19, next-export-optimize-images
    tsconfig.json has correct path aliases and strict mode
  </verify>
  <done>
    All dependencies installed in node_modules
    package.json includes all required packages
    next.config.mjs has output: 'export' configured
    tsconfig.json has @/* path alias configured
  </done>
</task>

<task type="auto">
  <name>Configure Tailwind CSS and create app directory structure</name>
  <files>
    tailwind.config.ts
    postcss.config.mjs
    src/app/layout.tsx
    src/app/page.tsx
    src/app/globals.css
  </files>
  <action>
    1. Create postcss.config.mjs with tailwindcss and autoprefixer plugins

    2. Create tailwind.config.ts with:
       - Content paths: src/pages/**/*, src/components/**/*, src/app/**/*
       - Extend theme with custom colors (placeholder for brand colors - user will provide)
       - Default configuration otherwise

    3. Create src/app directory structure:
       - src/app/layout.tsx (root layout with HTML structure, metadata, font optimization)
       - src/app/page.tsx (basic homepage component)
       - src/app/globals.css (Tailwind directives + base styles)

    4. In src/app/layout.tsx:
       - Set lang="tr" for Turkish content
       - Import Inter font from next/font/google
       - Add metadata for site (title, description)
       - Include suppressHydrationWarning on html tag (for next-themes compatibility)
       - Import globals.css

    5. In src/app/page.tsx:
       - Simple "Moka Dijital" heading as placeholder
       - Basic structure to verify routing works

    6. In src/app/globals.css:
       - Add @tailwind base/directives/utilities
       - Add basic CSS reset for consistent rendering
  </action>
  <verify>
    tailwind.config.ts exists with correct content paths
    src/app/layout.tsx has correct imports and structure
    src/app/globals.css has Tailwind directives
  </verify>
  <done>
    Tailwind CSS configuration is complete
    App directory structure exists (src/app/)
    Root layout renders with proper HTML structure
    Global styles import successfully
  </done>
</task>

<task type="auto">
  <name>Initialize shadcn/ui and create base component structure</name>
  <files>
    components.json
    src/components/ui/
    src/lib/utils.ts
    .eslintrc.json
    prettier.config.mjs
  </files>
  <action>
    1. Create components.json for shadcn/ui configuration:
       - Set tailwind config path
       - Set rsc: true (React Server Components)
       - Set tsx: true
       - Set component path: @/components
       - Set utils path: @/lib/utils
       - Use CSS variables for theming

    2. Create src/lib/utils.ts with cn utility function:
       - Import clsx and tailwind-merge
       - Export cn() function for merging Tailwind classes

    3. Create src/components/ui/ directory for shadcn components:
       - DO NOT install any components yet (user will specify as needed)
       - Just create the directory structure

    4. Create .eslintrc.json with:
       - extends: next/core-web-vitals
       - TypeScript rules enabled
       - Import alias configured (@/*)

    5. Create prettier.config.mjs with:
       - Single quotes
       - 2 space indentation
       - Trailing comma
       - Semicolons

    6. Update package.json scripts:
       - dev: next dev
       - build: next build
       - start: next start
       - lint: next lint
       - format: prettier --write .

    DO NOT install specific shadcn components yet - will be added as needed in later phases
  </action>
  <verify>
    components.json exists with correct configuration
    src/lib/utils.ts exports cn() function
    src/components/ui/ directory exists
    .eslintrc.json has next/core-web-vitals preset
  </verify>
  <done>
    shadcn/ui configuration initialized
    Utility function for class merging available
    ESLint and Prettier configured
    Project scripts are ready for development workflow
  </done>
</task>

<task type="auto">
  <name>Verify static export and development setup</name>
  <files>
    (no new files - verification task)
  </files>
  <action>
    1. Run TypeScript compiler to check for type errors:
       - Execute: npx tsc --noEmit
       - Should complete without errors

    2. Start development server:
       - Execute: npm run dev
       - Verify server starts on http://localhost:3000
       - Check console for no errors or warnings

    3. Run ESLint:
       - Execute: npm run lint
       - Should pass without critical errors

    4. Test static export build:
       - Execute: npm run build
       - Verify ./out directory is created with static HTML/CSS/JS
       - Check that output directory contains index.html and static assets

    5. Verify build output:
       - Check ./out/index.html exists
       - Verify no API routes were generated (should be static files only)
       - Confirm next-export-optimize-images processed images

    If any errors occur, fix configuration issues before marking task complete.
  </action>
  <verify>
    npx tsc --noEmit passes without errors
    npm run dev starts development server successfully
    npm run build creates ./out directory with static files
    ./out/index.html exists and contains static markup
  </verify>
  <done>
    Development server starts without errors
    TypeScript compilation passes
    Static export build completes successfully
    ./out directory contains static HTML/CSS/JS files
    Project is ready for phase 2 (page structure and content)
  </done>
</task>

</tasks>

<verification>
After completing all tasks, verify:

1. **Static Export Configuration**: next.config.mjs contains output: 'export' and builds to ./out directory
2. **Development Workflow**: npm run dev, npm run build, npm run lint all work without errors
3. **TypeScript Coverage**: All .ts/.tsx files compile without type errors
4. **Styling System**: Tailwind CSS is configured and globals.css imports successfully
5. **Component Foundation**: shadcn/ui is initialized and ready for component installation
6. **Form Dependencies**: React Hook Form, Zod, and related packages are installed

Run the following verification commands:
```bash
# Type checking
npx tsc --noEmit

# Linting
npm run lint

# Development server test
timeout 10 npm run dev || true

# Static export build test
npm run build

# Verify output directory exists
test -d ./out && echo "Static export successful" || echo "Build failed"
```
</verification>

<success_criteria>
Phase 1 setup is complete when:

1. Next.js 15 project with TypeScript and Tailwind CSS is initialized
2. Static export configuration (output: 'export') is working and builds to ./out
3. Development server runs without errors (npm run dev)
4. All dependencies are installed and package.json scripts are configured
5. shadcn/ui is initialized and ready for component installation
6. Project structure follows the recommended architecture (src/app/, src/components/, src/lib/)
7. ESLint, Prettier, and TypeScript configurations are in place
8. Form handling dependencies (React Hook Form, Zod) are installed for later use
9. next-export-optimize-images plugin is configured for build-time image optimization
10. Project is ready for Phase 2 (page structure and content creation)
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-setup/01-01-SUMMARY.md`
</output>
