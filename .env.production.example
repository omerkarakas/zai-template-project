# Production Environment Configuration
# =====================================
#
# SETUP INSTRUCTIONS:
# 1. Copy this file to .env.local
# 2. Update NEXT_PUBLIC_N8N_WEBHOOK_URL with your production webhook URL
# 3. Run production build: npm run build
#
# =====================================
# n8n WEBHOOK CONFIGURATION
# =====================================
#
# To get your production webhook URL:
#
# 1. Go to https://ai.mokadijital.com
# 2. Navigate to: Workflows -> Lead Registration - Moka Dijital (ID: XhpDEv1l5IgxWKPL)
# 3. Click the "Active" toggle (top right) to activate the workflow
# 4. Click on the Webhook node in the workflow
# 5. Copy the "Production URL" (NOT the Test URL)
# 6. Paste it below as NEXT_PUBLIC_N8N_WEBHOOK_URL
#
# The webhook will receive:
# - Form fields: isim, telefon, email, sirket, website, mesaj, service
# - Metadata: timestamp, page, source, userAgent, referrer

# Production webhook URL (replace with actual production URL from n8n)
NEXT_PUBLIC_N8N_WEBHOOK_URL=https://ai.mokadijital.com/webhook/lead-registration

# =====================================
# SITE CONFIGURATION
# =====================================

# Production domain
NEXT_PUBLIC_BASE_URL=https://mokadijital.com

# =====================================
# IMPORTANT NOTES
# =====================================
#
# - Never commit .env.local to version control (it's in .gitignore)
# - .env.local is used for both development and production builds
# - NEXT_PUBLIC_ prefixed variables are embedded in the build output
# - After updating webhook URL, rebuild: npm run build
# - Test the form submission after deployment to verify webhook receives data
